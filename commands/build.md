---
description: jediway build mode (Ground, Spec with Plan, Implementer, Verifier, Judge) for a change
argument-hint: <what must change, and why>
---

Load the jediway `way` skill and run it in build mode.

Task: $ARGUMENTS

State the mode, then Ground. Write `.jediway/spec.md`, show it, compose each child with `scripts/compose.sh` and show the composed prompt before sending. Finish with the judgement: what changed, how it was verified, what was rejected and why.
