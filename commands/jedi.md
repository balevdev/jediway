---
description: Run the jediway pipeline: brainstorm, then Planner, Implementer, Verifier, one after another
argument-hint: <what must change, and why>
---

Load the jediway `way` skill and run the pipeline.

Task: $ARGUMENTS

Brainstorm with me until the Mission, Done, Non-goals, and Scope are clear, write `.jediway/spec.md`, then dispatch the Planner. Show me the plan and wait once. After I approve, run the Implementer and the Verifier back to back and report the verdict.

You orchestrate and nothing else: do not read the repository, do not split the task, do not re-verify the Verifier. Brief each child with the four-line shape and carry only facts the next one cannot read from disk.
